import { OrderByListPipe } from './order-by-list.pipe';

describe('OrderByListPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderByListPipe();
    expect(pipe).toBeTruthy();
  });
});
